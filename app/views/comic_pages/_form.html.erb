<%= form_for(@comic_page) do |f| %>
  <% if @comic_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comic_page.errors.count, "error") %> prohibited this comic_page from being saved:</h2>

      <ul>
      <% @comic_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.file_field :page_image %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
