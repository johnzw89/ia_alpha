<p id="notice"><%= notice %></p>

<ul class="thumbnails">
  <% @comic_pages.each do |comic_page| %>
    <li class="span4">
      <div class="thumbnail">
        <%= image_tag comic_page.page_image.url(:medium) %>
        <%= link_to 'Show', comic_page %>
        <%= link_to 'Edit', edit_comic_page_path(comic_page) %>
        <%= link_to 'Destroy', comic_page, method: :delete, data: { confirm: 'Are you sure?' } %>

        <h3>Thumbnail label</h3>
        <p>Thumbnail caption...</p>
      </div>
    </li>
  <% end %>
</ul>

<%= link_to 'New Comic page', new_comic_page_path, class: "btn btn-small" %><br>
<%= link_to 'Edit', edit_comic_path(@comic) %> |
<%= link_to 'Back', comics_path %>







rails c

a = Comic.first.comic_pages
b = Comic.


/comics/1/comic_pages/

<%= form_for [@comic, Comic.first.comic_pages] do |f| %>
  <%= f.text_field :synopsis %>
  <%= f.submit %>
<% end %>

<form action="post" url="/comics/1/comic_pages"